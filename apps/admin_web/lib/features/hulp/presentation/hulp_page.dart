import 'package:flutter/material.dart';

class HulpPage extends StatelessWidget {
	const HulpPage({super.key});
	@override
	Widget build(BuildContext context) {
		return Center(child: Text('Hulp & Ondersteuning (stub)', style: Theme.of(context).textTheme.titleMedium));
	}
} 