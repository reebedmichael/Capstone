import 'package:flutter/material.dart';

class KennisgewingsPage extends StatelessWidget {
	const KennisgewingsPage({super.key});
	@override
	Widget build(BuildContext context) {
		return Center(child: Text('Kennisgewings (stub)', style: Theme.of(context).textTheme.titleMedium));
	}
} 