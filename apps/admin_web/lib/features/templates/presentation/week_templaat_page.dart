import 'package:flutter/material.dart';

class WeekTemplaatPage extends StatelessWidget {
	const WeekTemplaatPage({super.key});
	@override
	Widget build(BuildContext context) {
		return Center(child: Text('Week Templaat (stub)', style: Theme.of(context).textTheme.titleMedium));
	}
} 